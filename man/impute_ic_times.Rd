% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-ic.R
\name{impute_ic_times}
\alias{impute_ic_times}
\title{Draw event times for interval-censored subjects from the conditional hazard}
\usage{
impute_ic_times(object, ic, cut, beta = NULL, cache = NULL)
}
\arguments{
\item{object}{A fitted \code{\link{pamm}} model used as imputation model.}

\item{ic}{A data frame as returned by \code{\link{parse_ic_surv}} (subject-
level, with covariates and \code{ic_L}/\code{ic_R}/\code{ic_kind}).}

\item{cut}{The fixed vector of interval cut-points (shared across imputations).}

\item{beta}{Coefficient vector to evaluate the hazard at. Defaults to
\code{coef(object)}; pass a posterior draw for proper multiple imputation.}

\item{cache}{Optional pre-built cache from \code{\link{ic_pred_cache}}; avoids
recomputing the (expensive) design matrix across imputations.}
}
\value{
Numeric vector of (possibly imputed) event times, length
\code{nrow(ic)}.
}
\description{
For a fitted PAMM with piecewise-constant hazard, draws
\eqn{T_i \sim p(T \mid L_i < T \le R_i, x_i, \theta)} by inverting the
cumulative-hazard increment between \eqn{L_i} and \eqn{R_i}. Exact and
right-censored observations are returned unchanged (right-censored subjects
are \emph{not} imputed: they contribute correctly as censored at \code{ic_L}).
}
\keyword{internal}
