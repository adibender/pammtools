% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-ic.R
\name{ic_pred_cache}
\alias{ic_pred_cache}
\title{Build the subject-by-interval prediction grid used for IC imputation}
\usage{
ic_pred_cache(object, ic, cut, cause_levels = NULL, cause_var = "cause")
}
\arguments{
\item{object}{A fitted \code{\link{pamm}} model used as imputation model.}

\item{ic}{A data frame as returned by \code{\link{parse_ic_surv}} (subject-
level, with covariates and \code{ic_L}/\code{ic_R}/\code{ic_kind}).}

\item{cut}{The fixed vector of interval cut-points (shared across imputations).}

\item{cause_levels}{Optional character vector of competing-risk cause levels.
When supplied, one \code{lpmatrix} per cause is built and returned in
\code{X_list}.}

\item{cause_var}{Name of the cause column expected by the model.}
}
\value{
A list with the interval information \code{ii}, \code{n_int},
\code{n_sub}, and either a single design matrix \code{X} or a list
\code{X_list} (competing risks).
}
\description{
Constructs the (subjects \eqn{\times} intervals) grid on the \emph{fixed}
cut-points and evaluates the \code{lpmatrix} of the fitted PAMM once, so that
across imputations only the linear predictor (and hence the hazard) needs to
be recomputed for a new coefficient draw. Rows are subject-major: the first
\code{n_int} rows belong to subject 1, the next \code{n_int} to subject 2,
etc., so that \code{matrix(h, nrow = n_int)} has one column per subject.
}
\keyword{internal}
