% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make-newdata.R
\name{reconstruct_cutpoints}
\alias{reconstruct_cutpoints}
\title{Ensure all breakpoints are present in newdata for cumulative calculations}
\usage{
reconstruct_cutpoints(newdata, object, time_var, interval_length)
}
\arguments{
\item{newdata}{A data frame with a time column and, optionally, grouping.
Must carry the \code{trafo_args} and \code{intvars} attributes set by
\code{as_ped}.}

\item{object}{A fitted PAM/PAMM model object, passed to \code{expand_df}.}

\item{time_var}{Character name of the time variable (e.g. \code{"tend"}).}

\item{interval_length}{Character name of the interval-length column. If
absent from \code{newdata} it is created via \code{reconstruct_intlen}.}
}
\value{
A data frame with all required breakpoints present and an
\code{interval_length} column guaranteed to exist.
}
\description{
Checks whether all cut points up to the maximum observed time are present in
\code{newdata}. If not, expands the data frame to include the missing
breakpoints via \code{expand_df}. In either case the function guarantees that
an interval-length column (default \code{intlen}) exists on return.
Existing grouping is preserved after expansion.
}
\keyword{internal}
